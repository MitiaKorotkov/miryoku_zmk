# Mouse layer (movement, scroll, clicks) needs the pointing subsystem
# compiled in - it isn't enabled by default just because &mkp/&mmv/&msc
# behaviors exist in the keymap. Without this, those bindings silently
# do nothing at runtime (no build error either).
CONFIG_ZMK_POINTING=y
